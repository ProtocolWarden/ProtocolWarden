repo_key: ProtocolWarden
src_root: .
tests_root: tests

privacy:
  require_boundary_artifact: true

audit:
  # This repo's .console is reconciled + clean + under budget; enforce R1/R2.
  reconcile_enforce: true
